# Configuracion de Nginx para SIGES
# Agregar este bloque a tu configuracion de nginx

# Frontend - siges.johandev.lat
server {
    listen 80;
    server_name siges.johandev.lat;

    location / {
        proxy_pass http://siges_frontend:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# Con SSL (recomendado) - usar certbot o similar
# server {
#     listen 443 ssl http2;
#     server_name siges.johandev.lat;
#
#     ssl_certificate /etc/letsencrypt/live/siges.johandev.lat/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/siges.johandev.lat/privkey.pem;
#
#     location / {
#         proxy_pass http://siges_frontend:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
# }
